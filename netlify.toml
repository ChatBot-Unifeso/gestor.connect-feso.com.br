[build]
  # Diretório de onde o código será compilado
  base = "publish"
  # Comando para construir o site
  command = "yarn &&yarn build"
  # Pasta onde o comando de build será executado
  publish = "dist"